USING VIRTUAL ENVIRONMENTS
==========================
$ sudo pip install virtualenv
$ cd /tmp
$ virtualenv venv
$ source /tmp/venv/bin/activate
$ cd ~/minibus_src_dir
$ pip install .

USING PIP 'EDITABLE' MODE
=========================
$ cd ~/minibus_src_dir
$ sudo pip install -e .

PROFILING
=========
(Using example code)
$ cd examples/
$ python -m cProfile -s cumtime basic_publisher.py
